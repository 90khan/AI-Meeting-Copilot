{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only core permissions required for desktop lifecycle commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
